'use strict';

const target = document.querySelector('main');

target.innerHTML = `
    <h1 style="color: yellow;">Modify and save</h1>

    <code><pre>/examples/hmr/example.js</pre></code>

    to update this element without reloading the page.`;